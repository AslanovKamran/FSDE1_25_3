📚 Arrays & Vectors

Сегодня познакомимся с уже готовыми классами по работе с массивами - Array (для статических массивов) и Vector (для динамических)

Данные классы инкапсуоируют логику работы с коллекцией данных, предоставляя удобный интерфейс для взаимодействия. Например: удаление, добавления, перераспределения памяти под элементы. Здесь нам понадобятся фундаментальные знания, приобретенные на предыдущих уроках.

Для начала разберемся со статическим массивом данных. Кроме самих элементов мы всегда радом с ним передавали также и его размер. Это нужно в тех случаях когда я массив передавал в ф-цию или запускал цикл

    const int size = 5;
    int arr [size] = {1,2,3,4,5};

Это подход языка С. В языке С++ есть навороченные подходы для работы с массивами через классы. Такого подхода мы будем придерживаться и в дальнейшем.

📝 Класс array - шаблонный класс для работы со статическим массивом фиксированного размера.
Для начала работы с ним нужно подключить библиотеку 

    #include <array>

Данная библиотека предоставляет нам новый тип данных - класс array. Ситаксис такой:

    array<(Type),(Size)> (name)

Например

    array<int,5> nums = {1,2,3,4,5};

В чем отличие такого подхода от того, что мы делали раньше. Во первых теперь nums - это не просто массив, а объект класса array обладающий данными и поведением. 

🧑‍💻 Рассмотрим некоторый базовый функционал данного класса:


    array<int, 5> nums = {1, 2, 3, 4, 5};

    cout << "Size of array: " << nums.size() << endl;
    cout << "First element: " << nums.front() << endl;
    cout << "Last element: "  << nums.back() << endl;
    cout << "Third element: " << nums.at(2) << endl; // безопасный доступ

Также в нем существуют базовые проверки границ массива, так например:

    array<int, 3> nums = { 1, 2, 3};
    int oldStyleNums [3] = { 1,2,3 };

    cout << nums[5];         ❌ Error
    cout << oldStyleNums[5]; ✅ Garbage

Это то что касается статических массивов. 


🌀 В дополнение к этому рассмотрим еще один метод обработки элементов массива. 
Сейчас мы проходимся по массиву через цикл for. Есть еше одна альтернатива похожая на то как мы обращались к массиву в пайтоне. Она нам поможет обращаться к элементам массива напрямую а не через индексы. Для этого используется сниппет rfor + Tab

    for (auto& number : nums)
    {
        cout << number << ' ';
    }

Такой подход есть фактически в любом языке программирования в том или ином виде. Так например в C#, Java, JavaScript такой цикл называется foreach.
Данный цикл выводит все элементы массива от самого начала до самого конца.


📝 Перейдем теперь к динамическим массивам - класс vector.

vector — это шаблонный класс, реализующий динамический массив.
В отличие от std::array, его размер может меняться во время выполнения программы: можно добавлять и удалять элементы без явного управления памятью.

    #include <vector>

Принцип использования такой же как и в статических, за исключением того, что размер указывать не нужно.

    vector<int> nums = { 1, 2, 3};
    for (auto& number : nums)
    {
        cout << number << ' ';
    }


🔹 Основные методы

1. Добавление и удаление

    nums.push_back(10);     // добавить элемент в конец
    nums.pop_back();        // удалить последний элемент
    nums.clear();           // очистить весь вектор
    nums.assign(10,50);     // 10 раз вставить число 50

2. Доступ к элементам

    cout << nums[0];        // доступ без проверки (как в массиве)
    cout << nums.at(0);     // доступ с проверкой (бросает исключение при ошибке)
    cout << nums.front();   // первый элемент
    cout << nums.back();    // последний элемент

3. Размер и ёмкость

    cout << nums.size();      // текущее количество элементов
    cout << nums.capacity();  // сколько элементов может храниться без перевыделения
    cout << nums.empty();     // true, если вектор пустой




💡 Особенности класса vector

1. Автоматическое управление памятью
Когда элементов становится больше, чем capacity, вектор перевыделяет память (обычно увеличивая её в 1,5-2 раза) и копирует туда все данные.

2. Элементы хранятся в памяти подряд
Это значит, что vector можно безопасно передавать в функции, где требуется C-массив

3. Высокая скорость операций

🧠 Методы pop_back и push_back соответсвенно удаляют и добавляют элемент в конец массива. Возможны случаи когда мне понадобится вставить элемент в середину массива или удалить n-ый элемент. Тогда эти методы мне не подойдут. Для таких ситуаций существуют 2 других метода, принцип работы которых мы сейчас не поймем, но синтаксис их показать можно уже сейчас:

    nums.insert(nums.begin() + 1, 20); // вставить 20 на вторую позицию
    nums.erase(nums.begin() + 1);      // удалить элемент на второй позиции

nums.begin() — это итератор (или «умный указатель»), который указывает на первый элемент вектора. Многие методы vector работают именно с итераторами, а не с "обычными индексами".
nums.begin() — позиция первого элемента под нулевым индексом аналог &nums[0]. И на них также действует арифметика указателей:

    vector<int> nums = { 11, 22, 33};

    cout <<*(nums.begin() + 0) <<endl;
    cout <<*(nums.begin() + 1) <<endl;
    cout <<*(nums.begin() + 2) <<endl;

Таким образом вместо вставки элемента в середину массива будем использовать такую запись:
     
     nums.insert(nums.begin() + 1, 20);     ✅
     nums.insert(1, 20);                    ❌

Или 

    vector<int> nums = { 11, 22, 33};
    
    nums.erase(nums.begin() + 1);

    for (auto& i : nums)
    {
        cout << i << endl;
    }

🧑‍💻 Кроме указателя на начало массива есть еще и указатель на его конец - nums.end(); Если быть более точным, то end указыват на область памяти ВНЕ массива. Поэтому для доступа к самому последнему элементу я буду использовать такую арифметику указателей:


    vector<int> nums = { 11, 22, 33};
    
    //cout <<*(nums.end()) << endl;     // Out of range error
    cout <<*(nums.end() - 1) << endl; // 33
    cout <<*(nums.end() - 2) << endl; // 22
    cout <<*(nums.end() - 3) << endl; // 11

Можно пользоваться хоть первым, хоть вторым указателем. И не обязательно в отделности. Так например у метода erase есть перегруженный вариант, который принимает 2 параметра: указатель на начало и на конец (не включительно). При вызове данного метода элементы между этими указателеми удалятся из памяти. Так например для того, чтобы удалить элементы из массива, начиная с 3го элемента:

    vector<int> nums = { 11, 22, 33, 44, 55, 66, 77, 88, 99 , 110 };

    for (auto& i : nums)
    {
        cout << i << " | ";
    }
    cout << endl;

    nums.erase(nums.begin() + 2, nums.end());

    for (auto& i : nums)
    {
        cout << i << " | ";  // 11 и 22
    }


💡 Являясь шаблонной коллекцией вектор может содердать данные любого типа: даже нашего пользовательского класса.

    Person p1 = Person("Kamran", 25)
    Person p2 = Person("Ali", 20)
    vector<Person> people = {p1, p2};

    for (auto& person : people)
    {
        person.getInfo();
    }

Очень релко вам может пригодится создавать конструкции типа массива массивов. Через векторы это можно сделать таким образом:

    vector<vector<int>> matrix = 
    {
        {1,2,3},
        {4,5,6},
        {7,8,9}
    };


Кол-во элементов в каждой строке может быть разным (например на второй строчке вместо 3х элементов можно прописать 4)
И его также можно вывести через вложенный цикл for либо rfor(что чуть менее удобно)

    for (auto& outer : matrix)
    {
        for (auto& inner : outer)
        {
            cout << inner << " ";
        }
        cout << endl;
    }

    //

    for (int i = 0; i < matrix.size(); i++)
	{
		for (int j = 0; j < matrix[i].size(); j++)
		{
			cout << matrix[i][j] << " ";
		}
		cout << endl;
	}


🧠 Теперь мы можем свободно работать как с текстом, так и с динамическими массивами, не задумываясь о внутренней ее реализиции.

Задача:

Задание: Создание программы для тестирования
Основное задание (до 10 баллов)

Необходимо написать программу на C++, которая позволяет пройти тестирование.

Требования:

    1. Программа должна содержать 5 вопросов.
    2. У каждого вопроса должно быть от 2 до 5 вариантов ответа.
    3. Только один вариант является правильным.
    4. После завершения теста программа должна:
        • вывести количество правильных и неправильных ответов,
        • для каждого неправильного ответа показать, какой вариант был правильным.



Подсказка (структура классов):

    class Question
    {
        string text;                 // текст вопроса
        vector<string> answers;      // варианты ответа (2–5 вариантов)
        int correctAnswerIndex;      // индекс правильного ответа
        double score;                // количество баллов за вопрос
    };

    class Quiz
    {
        vector<Question> questions;  // список вопросов
        int correctAnswers;          // количество правильных ответов
        vector<int> wrongAnswers;    // индексы неправильных ответов (для статистики)

        void run();                  // запуск теста: задает вопросы и собирает ответы
        void showStatistics();       // выводит количество правильных/неправильных ответов
                                    // и показывает правильные варианты для ошибок
    };


Дополнительные задания (повышенный уровень)

🏆 Выполняя их, можно получить дополнительные баллы:

    Расширить количество вопросов до 20–30. (Можно сгенерировать на сторонних сервисах или придумать свои.)
    Сделать так, чтобы при каждом запуске программы вопросы выводились в случайном порядке.
    Добавить поле difficultyLevel у каждого вопроса.

        • Лёгкий вопрос = 0.5 баллов
        • Средний вопрос = 1 балл
        • Сложный вопрос = 1.5 балла  
