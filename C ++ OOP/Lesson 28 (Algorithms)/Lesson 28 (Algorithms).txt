📚 Алгоритмы в STL

Для работы с алгоритмами подключаем необходимые библиотеки:

    #include <algorithm>
    #include <numeric>


Алгоритмы позволяют удобно работать с коллекциями, избавляясь от рутины при написании циклов. Подробную документацию можно найти на cppreference .

🔹 Пример: проверка условий для всех элементов

Пусть у нас есть вектор из рандомнных чисел.

    vector<int> numbers = {55,22,11,77,33,44}



Нам нужно проверить, все ли числа больше 20. Без алгоритмов пришлось бы писать так. Без использования готовых ф-ций нам бы пришлось запустить цикл внутри которого нам пришлось бы проверять каждый элемент коллекции с условием:

    bool isOk = true;
    for(int i = 0; i < container.size(); i ++)
    {
        if(container[i] < 20)
        {
            isOk = false;
            break;
        }
    }

    cout << (isOk ? "Yes" : "No") << endl;


💡 От всех этих рутинных задач можно избавиться используя ф-ции из библиотеки algorithm.

🔹 all_of
Так на пример для нашей задачи существует ф-ция all_of проверяющая соответствуют ли все элементы коллекции какому-то условию.

Синтаксис таков:

    bool all_of(InputIt first, InputIt last, UnaryPredicate p);

    first — итератор на начало диапазона
    last — итератор на конец диапазона
    p — предикат (функция), которая возвращает bool для каждого элемента


Самый интересный здесь - третий параметр. Предикат это ни что иное как указатель на ф-цию. На место параметра p мы будем подставлять имя нашей ф-ции, которую мы заранее пропишем:

    bool MoreThan20(int x)
    {
        return x > 20;
    }

И теперь вот как выглядит наш код:

    bool result = all_of(numbers.begin(), numbers.end(), MoreThan20);
    cout << (result ? "Yes" : "No") << endl;


💡 Что происходит под капотом:

    • Алгоритм начинает с элемента, на который указывает begin().
    • Передает элемент в предикат (MoreThan20).
    • Если предикат возвращает false, алгоритм завершает проверку (касательно ф-ции all_of)
    • Иначе итератор смещается к следующему элементу.
    • Процесс повторяется до конца диапазона или до первого false.

Вместо нашей ф-ции MoreThan20 можно подставить любую другую, отвечающую требованиями предиката.

🧠 В дальнейшем можно будет еще сильнее упростить запись не создавая для этого отдельной ф-ции:

    bool result = all_of(numbers.begin(), numbers.end(), [](int x) {return x > 20});
    cout << (result ? "Yes" : "No") << endl;

Данная запись называется лямбда выражением. Их мы рассмотрим на следующем уроке.


📃 Остальные полезные функции из библиотеки <algorithm>


🔹 any_of
В отличие от аналога выше, данная ф-ция вернет true если хотя бы 1 элемент из коллекции удовлетворяет заданному условию.


    bool LessThan15(int x)
    {
        return x < 15;
    }

    int main()
    {
        vector<int> numbers = {55, 22, 11, 77, 33, 44};

        bool result = any_of(numbers.begin(), numbers.end(), LessThan15);

        cout << (result ? "True" : "False") << endl;
    }


🔹 none_of
Данная ф-ция вернет true если ни один элемент из коллекции не удовлетворяет заданному условию.

    bool IsNegative(int x)
    {
        return x < 0;
    }

    int main()
    {
        vector<int> numbers = {10, 20, 30, 40};

        bool result = none_of(numbers.begin(), numbers.end(), IsNegative);

        cout << (result ? "All positives" : "At least one negative") << endl;
    }

🔹 count и count_if

Функция count подсчитывает, сколько раз встречается конкретное значение,
а count_if — по условию.


    bool GreaterThan25(int x)
    {
        return x > 25;
    }

    int main()
    {
        vector<int> numbers = {10, 20, 30, 40, 30, 50};

        int count30 = count(numbers.begin(), numbers.end(), 30);
        int countGreater = count_if(numbers.begin(), numbers.end(), GreaterThan25);

        cout << "Число 30 встречается " << count30 << " раз(а)" << endl;
        cout << "Чисел больше 25: " << countGreater << endl;
    }


🔹 find

Ищет первое вхождение заданного значения и возвращает итератор на найденный элемент.

    int main()
    {
        vector<int> numbers = {10, 20, 30, 40};

        auto iter = find(numbers.begin(), numbers.end(), 30);

        if (iter != numbers.end())
            cout << "Элемент найден: " << *iter << endl;
        else
            cout << "Элемент не найден" << endl;
    }


🔹 sort

Сортирует коллекцию в порядке возрастания (по умолчанию) в заданном диапазоне. Исходный массив изменяется.

    int main()
    {
        vector<int> numbers = {44, 11, 33, 22};

        sort(numbers.begin(), numbers.end());

        cout << "Отсортированные элементы: ";
        for (int i = 0; i < numbers.size(); i++)
            cout << numbers[i] << " ";
    }


⚙️ Поведение count, find, sort при работе с объектами классов

Когда вы работаете не с примитивными типами (int, double, string), а с объектами пользовательских классов, STL-алгоритмы должны понимать, как сравнивать эти объекты между собой.

Каждая функция из STL делает это по-своему — но все они опираются на операторы сравнения (==, < и т.п.).


🔹 count и find

Эти функции сравнивают элементы на равенство.
Следовательно, они используют оператор ==.


    class Person
    {
    public:
        string name;
        int age;

        Person(string n, int a) : name(n), age(a) {}

        bool operator==(const Person& other) const
        {
            return name == other.name && age == other.age;
        }
    };

    int main()
    {
        vector<Person> people = {
            {"Tom", 20},
            {"Jerry", 25},
            {"Tom", 20}
        };

        Person target("Tom", 20);

        int result = count(people.begin(), people.end(), target);
        cout << "Найдено: " << result << endl;

        auto it = find(people.begin(), people.end(), target);
        if (it != people.end())
            cout << "Найден: " << it->name << " " << it->age << endl;
    }


✅ Результат:
Найдено: 2
Найден: Tom 20


🔹 sort

Функция sort() сравнивает элементы между собой с помощью оператора <,
если вы не указали собственную функцию сравнения.

    class Person
    {
    public:
        string name;
        int age;

        Person(string n, int a) : name(n), age(a) {}

        bool operator<(const Person& other) const
        {
            return age < other.age;
        }
    };

    int main()
    {
        vector<Person> people = {
            {"Tom", 20},
            {"Jerry", 25},
            {"Anna", 18}
        };

        sort(people.begin(), people.end());

        for (auto& p : people)
            cout << p.name << " " << p.age << endl;
    }


✅ Результат:
Anna 18
Tom 20
Jerry 25


🔹 Альтернатива — передать компаратор вручную

Если вы не хотите перегружать операторы или сделать это не является достижимым, можно просто передать функцию сравнения как третий параметр:

    bool CompareByName(const Person& a, const Person& b)
    {
        return a.name < b.name;
    }

    sort(people.begin(), people.end(), CompareByName);

    ✅ Теперь сортировка будет по имени, без изменения самого класса.



🧹 Удаление элементов по условию из контейнера (например, удалить все нули)
Прямого такого алгоритма в STL нет. Однако добиться этого можно с помощью 2ух других ф-ций.


Алгоритм remove_if не удаляет элементы физически, а лишь “сдвигает” ненужные элементы в конец контейнера и возвращает итератор на новую “границу”.

А метод erase удаляет все элементы из заданного диапазона.

Совместив эти две ф-ции можно сначала подвинуть все ненужные элементы в конец и получить указатель на начало цепи ненужных элементов. 

Затем начав с этого указателя до конца контейнера начать удалать данные.

Вот как это работает 👇



    bool IsZero(int x)
    {
        return x == 0;
    }


    int main()
    {
        vector<int> numbers = {1, 0, 5, 0, 3, 0, 7};

        // remove_if "сдвигает" ненужные элементы (нули) в конец
        auto newEnd = remove_if(numbers.begin(), numbers.end(), IsZero);

        cout << "After removing\n\n";

        for (int n : numbers)
            cout << n << " ";
       

        // Теперь удаляем “хвост”
        numbers.erase(newEnd, numbers.end());

        cout << "\n\nAfter erasing\n\n";

        for (int n : numbers)
            cout << n << " ";
    }

🔸 Что здесь происходит

    remove_if проходит по всем элементам.

    Все элементы, не подходящие под условие, “сдвигаются” в начало.

    Функция возвращает итератор на начало области, содержащей сдвинутые (неподходящие) элементы.

    Мы вызываем erase от этого итератора до end(), чтобы физически удалить хвост.


Однако следует отметить, что большинство коллекций уже поддерживают удаление элементов:

  list<int> numbers = {1, 5, 3, 5, 7, 5, 9};
    numbers.remove(5); // сразу удаляет все 5-ки

    for (int n : numbers)
        cout << n << " ";