📚 Динамические структуры данных: Связный список

Ближайшие несколько занятий мы с Вами будем разбирать такую фундаментальную тему как динамические структуры данных. Данная тема касается не только языка С++, но и всех других языков программирования.

Структуры данных в программировании - это различные способы хранения данных в оперативной памяти. Данные способы хранения можно разделить на 2 классификации:
примитивные и непримитивные

1️⃣ К примитивным относятся обычные переменные, в которых могут хранится одни единственные значения.
Примеры: int, float, bool, char и т.п.


2️⃣ К непримитивным относятся всяческие коллекции - содержащие набор данных. Из всех таких структур мы работали с массивами - самые распространенные коллекции во всех языках программирования.

💡 Массив - именованный набор однотипных данных, расположенных в памяти непрерывно.

Помимо массивов есть и другие типы коллекций, где данные хранятся немного по другому принципу и работают слегка иначе. Среди них:

    Linked List
    Stack
    Queue
    Tree, Graph

И в некоторых ситуациях иногда предпочтительнее работать с иными коллекциями нежели с массивами. Ни один вид коллекций не лишен своих недостатков. Какие-то структуры работают медленее для чтения, но быстро для записи, некоторые наоборот и д.р.

👨‍🏫 Так например самым большим плюсом массива является быстрое чтение: не важно по какому индексу мы прочли данные по 1му или по 1001му, а вот процессы вставки очень затратны. И в случае если нам за раз нужно вставить несколько сотен записей в наш массив, то мы просядем по производительности. В этом случае нам подойдут другие коллекции, а именно List.

Или например, если нужно хранить данные в отсортированном виде или же часто производить поиск данных. В этом случае нам может понадобиться коллекция типа Tree. 

🧩 Данные структуры данных встречаются во всех языках программиорования, поэтому знать принцип их работы нужно обязательно. Их реализацию мы рассмотрим на примере языка C++, а в дальнейшем в C# мы уже будем пользоваться готовыми решениями из коробки.

👨‍🏫 Перед тем как перейти к новым структурам данных вспомним то, как мы работаем с динамическим массивом. 
При создании массива у нас есть указатель на область памяти в heap. Все элементы в памяти расположены строго друг за другом как состав поезда. Для добавления массива нам нужно производить огромное кол-во операций. Это очень трудозатратно: и чем больше наш массив тем медленнее будет происходить вставка. Существуют некоторые приемы для частичной оптимизации процесса вставки - это использование поля capacity. С помощью данного подхода при создании массива на самом деле выделятся в 1,5х раз больше памяти, чем нужно, за счет этого до какого-то момента можно без затрат производить вставку элементов. Так работают классы string & vector. Память перевыделится в момент, когда capacity достигнет предела.

⚡ Однако и такой структуры данных есть один очень большой плюс - скорость чтения по индексу. Так как индекс это ничто иное как обычно число (а числами любого размера компьютер работает невероятно быстро), то нет разницы обращаеися мы к 5му или 1005му элементу. Дойти с нулевого индекса до них обоих программа может молниеносно. Так как все это работает на принципе арифметики указателей. К начальному (нулевому) адресу я сначала прибавлю 5, а потом 10005, что займет одно и тоже время.

И как раз таки есть структура данных, которая является полной ее противоположностью - со структурой данных, которая быстро вставляет, но медленно читает данные - Linked List. 

👉 Если я храню числа 11, 22, 33 в массиве то в памяти они у меня хранятся в таком виде

    *myArray -> 11 | 22 | 33

🧠 В чем смысл списка. Во первых вместо "голых" чисел я теперь храню так называемые узлы, которые также называют node. Каждый node в себе хранит значение value и указатель на следующий node. А имя списка хранит в себе указатель на 1ый node. И в такой структуре все эти nodes хранятся в отдельных кусках оперативной памяти, в отличие от массива, являющейся непрерывной областью данных.

    myList -> [11,pointer] -> [22, pointer] -> [33, nullptr]

🎯 В чем преимущество данной коллекции? Для добавления нового узла в любую позицию мне нужно лишь перенаправить указатель. То же самое справедливо и для удаления.

😐 В чем недостаток данной коллекции? Здесь уже арифметика указателей не сработает, так как элементы расположены не непрерывно. поэтому, чтобы добраться до 51го элемента в коллекции я по любому должен пройти через первые 50, перемещаясь от узла к узлу. Еще одним недостатком является размер самого узла. Голое число весило 4 байта, тогда как узел весит 4 + 8, где в 8 это размер указателя (в некоторых системах размер указателей = 4). Однако в долгосрочной перспективе это намного лучше чем в момент времени копировать все элементы старого массива в новый.

Перейдем к созданию данной коллекции. Как уже было сказано вместо голых чисел нам нужно держать что-то более комплексное а именно значение и указателей на следующий элемент. Для этого создадим класс который назовем Node

    class Node
    {
        int data;
        Node* next;
    };

Данный класс, хранит в себе 2 поля: значение - т.е. само число, а также указатель на точно такой же узел, который таже хранит эти же поля. 

🧠 Тут можно заметить, что никакие действия с данным классом я не произвожу. Все действия будут происходить в  другом классе под названием MyList, в состав которого войдет мой Node. Следовательно мы можем заменть класс на структуру, так как Node нам нужен только для ххранения данных. Никакого поведения в объекте Node не будет.

    struct Node
    {
        int data;
        Node* next;
    };

Перейдем теперь к созданию самого класса MyList. Как и было указано в начале сам list будет указывать на самый первый элемент в коллекции. Назовем его head.  Также будем в дополнительном поле хранить размер нашей коллекции.
В дополнение к этому пропишем сигнатуры методов для работы с нашей коллекцией

    class MyList
    {
        private:
            
            Node* head = nullptr;
            int size = 0;

        public:

            void pushBack(int data);
            void insert(int data, int position);
            void remove(int position);
            int getAt(int position);
    };
    


🔍 Начнем в метода pushBack. Причем нужно учесть 2 варианта, когда у нас коллекция пустая и уже содержит несколько элементов.
Если у нас коллекция пуста, то head указывает вникуда - nullptr. Что нам нужно сделать в этой ситуации:

    нужно создать новую структуру в памяти и перенаправить указатель head-a на него.
    назначить данному head-у новое значение которое пришло из параметра data
    в поле next записать nullptr (данную строку даже можно опустить, но для наглядности оставим)


    void pushBack(int data) 
    {
        
        if (head == nullptr)
        {
            Node* newNode = new Node();
            newNode->data= data;
            newNode->next = nullptr;
           
            //или
            //(*newNode).data = data;
            //(*newNode).next = nullptr;
           
            head = newNode;
        }
        else
        {
            //
        }
        size++;
    }


📍 Чуть сложнее ситуация, когда в списке уже есть данные и нам нужно вставить новый