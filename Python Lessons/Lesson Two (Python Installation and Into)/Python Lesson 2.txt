🐍 Python Introduction
Как мы помним, компьтеры не понимают человеческого языка. Для их работы они используют машшинный язык, состоящий из нулей и единиц.
К сожалению, этот язык очень далек от родного языка людей. Нам всем (и компьютерам, и людям) нужно чтото еще, общий язык для компьютеров и людей или мост между двумя разными мирами.

Нам нужен язык, на котором люди могут писать свои программы, и язык, который компьютеры могут использовать для выполнения программ.
Такие языки часто называют высокоуровневыми языками программирования.  Они чем-то похожи на естественные: они используют символы, слова и условные
обозначения, которые человек способен понять. Например в Python команда для печати данных назывется print, команда для условного разделения потока кода - if else и т.д.

Программа, написанная на высокоуровневом языке программирования, называется «исходный код» (source code).

🌐 Представим, что вы успешно написали программу.
Как мы убедим компьютер выполнить ее? Вам нужно перевести вашу программу на машинный язык.
Есть два разных способа преобразование программы из высокоуровневого языка программирования в машинный язык:

Представь, что у тебя есть книга, написанная на иностранном языке, которого ты не знаешь (например, на японском). А ты хочешь понять, что там написано.

Теперь сравним компилятор и интерпретатор с двумя разными способами перевода этой книги:

🛠 Компилятор
Как переводчик, который сначала переводит всю книгу целиком, а потом ты её читаешь.
Сначала переводит весь текст на твой язык (например, русский).
После перевода ты читаешь уже полностью переведённую книгу.
Если в книге была ошибка (например, непонятное слово), переводчик скажет об этом сразу, ещё до начала чтения.

🔍 Пример: C, C++, Rust, Go — языки, которые обычно используют компиляцию.

🧠 Интерпретатор
Как переводчик, который сидит рядом с тобой и переводит по одной строчке, пока ты читаешь.
Переводит построчно, прямо во время чтения.
Если он встретит непонятное слово, он скажет: "Стоп, дальше не могу переводить".
Работает медленнее, но удобно, когда нужно быстро попробовать что-то.

🔍 Пример: Python, JavaScript, PHP — языки, которые часто используют интерпретацию.

Python является интерпретируемым языком программирования. Если вы хотите программировать на Python, вам понадобится интерпретатор Python. Вы не сможете запустить код без него. 


😎 Почему Python особенный?

Как получилось, что программисты, молодые и старые, опытные и начинающие, хотят его использовать? 
Как так получилось, что крупные компании признали Python и создали свои флагманские продукты, используя его?
На это есть много причин 

• его легко учить — время, необходимое для изучения Python, обычно меньше, чем для многих других
• его легко использовать для написания нового программного обеспечения
• его легко понять — также часто легче понять чужой код, если он написан на Python;

Конечно, у Python есть и свои недостатки:
• он не супер-скоростной — Python не обеспечивает исключительную производительность;
• в некоторых случаях он может быть устойчив к некоторым более простым методам тестирования — это значит, что отладка кода Python может быть более сложной, чем в других языках; к счастью, и делать ошибки в Python 


✌ Существует два основных типа Python, которые называются Python 2 и Python 3.
Python 2 является более старой версией оригинального Python. 
Python 3 является более новой (точнее, текущей) версией языка. Он проходит свой собственный путь развития, создавая свои стандарты и привычки.

❌ Эти две версии Python не совместимы друг с другом.
Скрипты Python 2 не будут работать в среде Python 3 и наоборот.

👨‍🏫 Как установить и использовать Python
Image: Python Download.png
Image: Python Installation.png

Чтобы проверить текущую версию Pythona нужно командной строке написать
py --version


📝 Помимо множества полезных компонентов, стандартная установка Python 3 содержит очень простое, но чрезвычайно полезное приложение под названием IDLE. IDLE — это аббревиатура от Integrated Development
and Learning Environment (интегрированная среда разработки и обучения).

👩‍💻 Как написать и запустить вашу первую программу
Теперь пришло время написать и запустить вашу первую программу на Python 3. 
Первый этап — создать новый исходный файл и заполнить его кодом. Нажмите на File в меню IDLE и выберите New file.
IDLE открывает для вас новое окно. Вы можете использовать его, чтобы писать и изменять свой код. Это окно редактора. 
Окно редактора в настоящее время без названия, но начинать работу рекомендуется с названия исходного файла. Его нужно сохранить (например на рабочем столе) с расширением py.

Далее, для более удобного написания кода, рекомендуется скачать VS Code. Также нужно скачать соответствующие расширения для корректного запуска кода в редакторе.

Теперь нам нужно потратить какое-то время на объяснение того, что вы на самом деле видите и почему это выглядит именно так.
Как видите, первая программа состоит из следующих частей:
■ слово «print»;
■ открывающая круглая скобка;
■ кавычка;
■ строка текста: «Hello, World!»;
■ кавычка;
■ закрывающая круглая скобка

🧪 Вы, вероятно, неоднократно встречали термин «функция» на уроках математики. А еще вы, наверняка, можете вспомнить несколько названий математических функций, таких как синус или логарифм.
Так вот print также является функцией в Python отвечающей за вывод данных на экран.

Функция (в этом контексте) — это отдельная часть компьютерного кода, которая:

■ вызывает некоторый эффект (например, отправляет текст в терминал, создает файл, рисует изображение, воспроизводит звук и  т.д.); это нечто совершенно неслыханное в мире математики;

■ вычисляет значение или некоторые значения (например, квадратный корень значения или длины данного текста); это то, что роднит функции Python с математическими понятиями.

Эти функции могут иметь различное происхождение. Они могут быть частью самого пайтона, могут поступать из внешных библиотек, называемых модулями или быть написаны самим программистом. Чуть-чуть забегая вперед напишем функцию которая приветсвтвует пользователя по имени:

def greet(name):
    print("Hello", name)

greet("Aslan") 

К функциям есть ряд требований, которые они в идеале должны выполнять:
1. Название функции должно отображать ее суть. Имя функции print самоочевидно, то есть «вывести», «отобразить»
2. Функция должна делать только то, для чего она создана, так например кофе машина делающее кофе, не должна также делать для потребителя и бутерброд.
3. Также в рамках одной программы не может быть 2 функций с одинакавыми именами.

💡 Есть еще один очень важный, компонент функции — параметр(-ы). По аналогии с математическими функциям в sin(30 deg) 30 deg здесь является параметром функции sin. Без параметра данная функция не имеет практического примемения. Также и в программировании функции могут иметь (1 и более) или вовсе не иметь параметров.

Однако вне зависимости от того принимает ф-ция параметры или нет для ее вызова все равно используется пара круглых скобок.
Аргументы ф-ции передаются внутри этих скобок. Если их несколько то они перечисляются через запятую.

Говоря о нашей функции print. Есть ли у функции print() из нашего примера параметры? Конечно, есть, но кто они?
Единственный аргумент, переданный в функцию
print() в этом примере, — это строка: Hello world! 

❓ Что такое строка.
Строка это тип данных или же класс, говоря на языке программирования.
Примеры встроенных типов данных в Python:

str — строки
int — целые числа
float — числа с плавающей точкой
bool — логические значения
list, tuple, dict, set — коллекции

Тип данных определяет, какие значения может принимать объект и какие операции можно с ним выполнять.

Так например числа можно складывать, сравнивать, а bool использовать для логичского разделения кода.
Для того чтобы указасть пайтону, что я использую строку их нужно ограничить в кавычки.
Почти все, что помещено в кавычки, будет восприниматься буквально, не как код, а как данные

Что происходит, когда Python встречает вызов, подобный приведенному ниже
print("Hello World")

✅
1. Во-первых, Python проверяет, является ли указанное имя разрешенным (он просматривает свои внутренние данные, чтобы найти существующую функцию с этим именем; если поиск не удался, Python прерывает код);

2. во-вторых, Python проверяет, позволяет ли условие функции по количеству аргументов вызвать функцию именно таким образом (например, если конкретная функция требует ровно двух аргументов, любой вызов, передающий только один аргумент, будет считаться ошибочным и прервет выполнение кода);

3.  в-третьих, Python ненадолго оставляет код и переходит в функцию, которую вы хотите вызвать;

4.  в-четвертых, функция выполняет свой код, вызывает желаемый эффект (если он есть), оценивает желаемый результат(-ы) (если они есть) и завершает задачу;

5. и, наконец, Python возвращается к вашему коду (в точку сразу после вызова) и возобновляет его выполнение.

📠 Print подробнее:
1. Функция, которая отправляет данные на устройство вывода, в 99% это консоль (терминал).
2. Функция, которая работает с любым типом данных.
3. Функция, которая ничего не вычисляет.

❗ Вызо  функции в пайтоне это инструкция для компьтера что-то сделать.
Синтаксис Python довольно специфичен в этой области.
В отличие от большинства языков, Python требует, чтобы строка не содержала более одной инструкции. 
Т.е. такой запуск:
print(Hello) print(World)

Также при написании кода в правильной форме
print(Hello)
print(World)

Можно заметить, что print автоматически добавляет перенос на новую строку после завершения выполнения. Это поведение можно изменить. Рассмотрим это в дальнейшем. Если вызвать ф-цию print без аргументов, то она просто добавит пустую строку.
print(Hello)
print()
print(World)

Это не единственный способ добавить новую строку в консоли вывода
print("Hello\nWorld!")

Обратная косая черта (\) имеет особое значение при использовании внутри строк — это называется escape последовательностью.  

Слово «escape» (побег) здесь следует понимать буквально — это означает, что последовательность символов в строке ненадолго сбегает (очень ненадолго) и заменяется на специальную подстановку. 

Другими словами, обратная косая черта сама по себе ничего не значит, это лишь своеобразное объявление о том, что следующий символ после обратной косой черты также имеет другое значение. Буква «n», которая идет за обратной косой чертой, это сокращение от «newline» (новая строка). И обратная косая черта, и буква n формируют специальный символ, который называется «символ новой строки». Он заставляет консоль начать вывод с новой строки.

Также если существует необходимость печатать кавычки нужно также пользоваться символом \.
print("The book is called \"The War and peace\"") 

Такой код также будет напечатан неверно:
print("4\2=2") Для печати символа обратного слеша также перед ним нужно вставить \.

Пара символов \t втавляет табуляцию
print("Tab\ttest")


✏ До сих пор мы тестировали поведение функции print() либо без либо с одним аргументом. Однако ф-ция print модет принимать 2 и более параметоров. Рассмотрим данный пример:

print("Hello", "From", "The", "Other", "Side")

Аргументы разделены запятыми. В этом случае запятые, разделяющие аргументы, играют совершенно иную роль, чем запятая внутри строки.
■ если функция print() вызывается с двумя и более аргументами, она выводит их всех в одной строке;
■ функция print() ставит пробел между выведенными аргументами по собственной инициативе.

Мы можем изменить это стандартное поведение ф-ции print при передаче нескольких параметров.
Если в редакторе навести на ф-цию print, то можно заметить, что она также может принимать и другие аргументы, помимо тех, что мы хотим вывести на консоль. Нас в данном случае интересуют 2 из них: sep & end.

sep позволяет менять поведение print() при передаче нескольких аргументов в функцию. 
print("Hello", "From", "The", "Other", "Side", sep = "->")

end позволяет менять поведение при завершении выполнения ф-ции. По умолчанию это перенос на новую строку.
print("Hello")
print("World")

Также строки можно оборачивать и в одинарные кавычки. Разници в Python между ними никакой. Однако есть пару моментов которые нужно учитывать.

🧪
Когда использовать какие?
Главное правило — удобство и читаемость:

Если строка содержит одинарную кавычку — оберни в двойные:
print("I'm happy")

Если строка содержит двойную кавычку — оберни в одинарные:
print('He said "hi"')

Если внутри и те, и другие — тогда экранируй:
print("He said: \"I'm fine\"")

                                                                🕵️‍♂️ Типы данных

До этого мы работали лишь со строками. Перейдем теперь к числам.

🧠Давайте начнем с простого эксперимента — посмотрите на фрагмент кода ниже.
print("2")
print(2)

При запуске кода мы увидим 2 совершенно одинаковые выводы. 2 и 2.
Функция print() выводит их абсолютно одинаково, и для человека они тоже выглядят одинаковыми, но внутренне, в памяти компьютера, эти два значения хранятся совершенно по-разному: строка существует как набор букв, в то время как число преобразуется в машинное представление (набор битов). Функция print() выводит их в понятной для человека форме.

🔢 Числа, которые обрабатывают современные компьютеры, бывают двух типов:
■ целые, то есть те, которые лишены дробной части;
■ и с плавающей точкой, которые содержат (или могут содержать) дробную часть.

Оба типа чисел существенно различаются по тому, как они хранятся в памяти компьютера, и по диапазону допустимых значений.
Характеристика числового значения, определяющая его вид, диапазон и применение, называется типом. Или классом.
Так целые числа это класс Integer (int),  а дробные - Floating (float)

🧩 Целые числа
Рассмотрим вопрос о том, как Python распознает целые числа. 
Этот процесс очень похож на то, как вы бы написали их карандашом на бумаге — это просто ряд цифр, которые составляют число. Но с оговоркой — вы не можете вставлять символы, (например зарятые или пробелы) которые не являются цифрами внутри числа.

Взять, к примеру, число одиннадцать миллионов сто одиннадцать тысяч сто одиннадцать. Если бы вы прямо сейчас взяли в руку карандаш, вы бы написали число следующим образом: 11,111,111 или вот так: 11.111.111 или даже так: 11 111 111.

Понятно, что такая запись облегчает чтение, особенно когда число состоит из большого количества цифр. Тем не менее, Python таких вещей не понимает. Это запрещено. А вот что Python разрешает, так это использование подчёркивания в числовых литералах. Следовательно, вы можете написать это число так: 11111111 или вот так: 11_111_111.

Для отрицательных чисел нужно просто добавить знак минус перед их объявлением: -42
Для положительных чисел писать + не обязательно, но допускается.

🐬 Теперь поговорим о числах с плавающей точкой.
Это числа, у которых есть (или может быть) дробная часть после десятичной точки, и хотя такое определение очень плохое, этого, безусловно, достаточно для того, что мы будем разбирать. Каждый раз, когда мы используем такой термин, как «два с половиной» или «минус нуль целых четыре» мы имеем в виду числа, которые компьютер воспринимает как числа с плавающей точкой: 2.5 -0.4. Обязательное замечание, разделение целой и дробной частей производится исключительно ТОЧКОЙ.   

Для записи "ноль целых и ..." можно использовать однин из 2х вариантов:
0.4 или .4

Также python будет совершенно по разному воспринимать 2 таких числа.
4 и 4.0.

Для указания экспоненты можно использовать такую запись:
print(3E8)
это краткая запись фразы «столько-то, умноженное на десять в такой-то степени»



👻 Булевые значения. 
Они не так очевидны, как предыдущие, так как используются для представления очень абстрактного значения — правдивости.
Каждый раз, когда вы спрашиваете Python, является ли одно число больше, чем другое, этот вопрос приводит к созданию некоторых конкретных данных — логических (булевых) значений. 

Джордж Буль дал  определение булевой алгебры — это раздел алгебры, использующий только два значения: правда и ложь, которые обозначаются как 1 и 0, соответственно.

Программист пишет программу, а программа задает вопросы. Python выполняет программу и дает ответы. Программа должна уметь реагировать в соответствии с полученными ответами. К счастью, компьютеры знают только два вида ответов:
■ Да, это правда;
■ Нет, это ложь

Вы никогда не получите ответ типа: «Я не знаю» или «Наверное, да, но точно не знаю». У этих логических значений есть строгие обозначения в Python: True (Правда) и False (Ложь)

print(10 > 11)
print(10 < 11)


➕✖
➖➗

Перейдем к такой теме как операторы.
Если запустить такой код

print(2+2)

То в консоли мы увидим 4. Это потому, что сама ф-ция Print посчиьтала значение выражения 2 + 2 и вывела ее ответ. 
Здесь:

2 является операндом
+ является оператором
2 + 2 явлется выражением 
4 является результатом выражения.

Оператор — это символ языка программирования, который оперирует значениями. Так например в арифметике знак + является оператором сложения 2х чисел.
Вы всегда складываете 2 числа. Даже в таком большом выражении.

2+9+6+3+21+53 

мы в момент времени всегда складываем 1 пару чисел.

Операторы использующие в работе 2 операнда называются бинарными. Таким образом можно понять, что + - * /, < >  это бинарные операторы. (- может быть и унарным, но об этом позже)

Рассмотрим все операции по порядку.

➕  Складывает числа, соединяет строки:

print(5+9) -> 14
print("Hello" + "World") -> "HelloWorld"

➖ Вычитает числа друг от друга. Нельзя вычитывать строки:
Может быть применен к одному или к двум операндам.

print(-4) унарный
print(8-4) бинарный

✖ Умножает числа, дублирует строки:
print("Hi"*3) -> HiHiHi
print(4*12) -> 48

❗ Если аргументы по обе стороны от вышеперечисленных операций целые числа, результат тоже будет целым числом. Если хотя бы один из них это дробное число, то - дробным

➗ Делит 2 числа, строки делить нельзя. Нельзя делить на ноль.
Всегда возвращает float  number.

print(6/4) -> 1.5
print(6/3) -> 2.0

Это были самые простые мат. операции. Теперь перейдем к более специфическим.

💪 Возведение в степень осуществляется с помощью **
Так 2**3 будет 2 в 3ей степени. 
print(2**3) = 8

💡 Если при делении нам необходимо получить именно целое значение, то вместо / нужно использовать //. Данная операция округляет число в меньшую сторону. Так например если при делении я получил число 

1.5 ответ будет 1.

А если я получил -1.5, то ответ будет -2, так как -2 меньше чем -1.

print(6//4) -> 1
print(6/4) -> 1.5

Однако если я явно укажу нулевую дробную часть при таком делении, то ответ также будет дробным


🤯 MOD

Следующий оператор довольно своеобразен, потому что он не имеет эквивалента среди традиционных арифметических операторов. Его графическое представление в Python выглядит как % (знак процента)

Результатом оператора является остаток после целочисленного деления. Другими словами, это значение, которое остается после деления одного значения на другое.

print(14 % 4) = 2

Так как 

■ 14 // 4 будет 3 — это целое число, частное;
■ 3 * 4 будет 12 — как результат умножения частного на делитель;
■ 14 – 12 будет 2 — это остаток.

print(12 % 4.5) -> 3.0. 
Правило все еще работает


🔗🔗🔗 Приоритеты операторов:

До сих пор мы рассматривали каждый оператор так,будто он не связан с остальными. Конечно, такая идеальная ипростая ситуация это редкость в реальном программировании.Кроме того, очень часто можно встретить более одного оператора водном выражении, и тогда предсказаниерезультата уже не столь очевидно.Рассмотрим следующее выражение:

2 + 3 * 5 = 17
Сначала нужно умножить3 на 5, запомнить результат — 15, после этого прибавитьк 15 2, и в итоге получить 17.

Феномен, который заставляет некоторые операторыдействовать раньше других, называется иерархией приоритетов.

Python точно определяет приоритеты всех операторови предполагает, что операторы с более высоким приоритетомвыполняют свои операции раньше операторовс более низким приоритетом.

Итак, если вы знаете, что у умножения * более высокий приоритет,чем у сложения +, результат должен бытьдля вас очевидным.

Если и подряд идущих операторов одинаковый приоритет, то они вычисляются по порядку слева направо.

print(9 % 6 % 2)
print(2 ** 2 ** 3)

Конечно, всегда можно использовать скобки, которые могут изменить естественный порядок вычислений. В соответствии с арифметическими правилами, подвыражения в скобках всегда вычисляются первыми. 

Вы можете использовать столько скобок, сколько вам нужно, и они часто используются для улучшения читабельности выражения, даже если они не меняют порядок операций.

print(2+2*5) -> 12 
print((2+2)*5) -> 20


📦 Variables (Переменные)

Абсолютно логичным будет вопрос о том, как хранить результаты этих операций так, чтобы использовать их в других операциях и так далее. Как сохранить промежуточные результаты и использовать их снова для получения последующих? Python справится и с этим. Он предлагает специальные «контейнеры», которые называются «переменными». Значения внутри этих контейнеров может меняться.

Каждая переменная имеет:

1. название - имя переменной, через которое мы сможем потом к ней обращаться.

2. значение - содержимое контейнера, которое можно извлечь или изменить.

Если хотите дать переменной имя, вы должны соблюдать некоторые строгие правила, которые называются конвенциями.

✅
myvar = "John"
my_var = "John"
_my_var = "John"
myVar = "John"
MYVAR = "John"
myvar2 = "John"

❌
2myvar = "John"
my-var = "John"
my var = "John"
False = "John"


💡 Те же ограничения относятся и к именам функций.

📥Что можно поместить в переменную? 

Что угодно.
Переменная может хранить любое значение любого типа
данных. Значение переменной — это то, что вы положили в нее. Оно может меняться так часто, как вам нужно. Сначала она может быть целым числом, а через секунду — числом с плавающей точкой, которое в итоге становится строкой. 

Теперь поговорим о двух важных вещах — как создаются переменные и как поместить в них значение (точнее, как им передать значения).

age = 17
print(age)

Данный код состоимт из двух простых инструкций:
■ Первая создает переменную с именем age и присваивает ей  значение  1.
■ Вторая выводит значение переменной в консоль.

Можно использовать столько объявлений переменных, сколько вам нужно для достижения цели, например:

name = "Kamran"
age = 25
work = "IT Instructor"

print(name, age, work)


Нельзя взаимодействовать с несущ. переменной.

number = 17
print(Number)

😮 Присвоение нового значения уже существующей переменной.
Просто нужно использовать знак равенства.
Знак равенства — это, на самом деле, оператор присваивания. 

Он присваивает значение своего правого аргумента левому, в то время как правый аргумент может быть произвольно сложным выражением.

number = 17
print(number)

number = 20
print(number)

🤯 Минута жести

Рассмотрим такой код
age = 10

❗age = age + 5

print(age) -> 15

Если бы математик увидел такую запись, он, наверное, возразил бы — ни одно значение не может быть равным самому себе плюс 5.

Но Python воспринимает знак = не как «равно», а как «присвоить значение».


👨‍💻 Вичислить длину гипотенузы c если катеты a, b соответственно равны 4 и 5

Еще одна особенность операторов в python.
Такую запись
age = age + 5

Можно сократить до 

age += 5 

⌨ Нередко возникает необходимость использовать переменные внутри print с каким-то текстом.

Например
name = "Kamran"
age = 17

print("Hi, my name is __. I'm __  years old")

Для того, чтобы выводить переменные внутри print нужно использовать форматированный вывод.

name = "Kamran"
age = 17

print(f'Hi, my name is {name}. I am {age} years old').

🔣 До этого мы только работали со статическими данными объявленными в коде. А что, если нужно получить данные от самого пользователя, например попросить его ввести свои данные, свою зарплату и посчитать бонусы и т.п.

Данная функция является полным зеркальным отражением ф-ции print и назывется input.

Функция input() считывает введенные пользователем данные и возвращает те же данные в работающую программу.
Программа может манипулировать данными, добавляя интерактивности в код.

Практически любая программа читает и обрабатывает данные. Введенные данные мы можем сохранить уже в знакомые нам переменные. 
Делается это так.

print("Input your name here...")
name = input()
print(f"Nice to meet you {name}")

Также данная функция сама в себе может хранить текст, который мы хотим показать пользователю перед вводом.

print("Input your name here...")
name = input()
print(f"Nice to meet you, {name}")

Этот вариант вызова функции input() упрощает код и делает его более понятным.

📤 У input-a есть одно сущесвтвенное ограничение: результат функции input это всегда строка. Строка, которая содержит все символы, введенные пользователем с клавиатуры. Это не целое число и не число с плавающей точкой.

Это значит, что ее не в сыром виде нельзя использовать в качестве
аргумента для арифметических операций.

prompt = input("Enter a number: ")
result = prompt * 2
print(f"Result is {result}")

Здесь у нас произойдет повторение строк. То есть то число которое введет пользователь будет воспринята как строка и при умножении строки на число мы просто получим строку повторенную N раз.

Есть ли решение этой проблемы? Конечно, есть.

♻ Нужно просто завернуть input в тот тип данных который мы хотим забрать от пользователя.

prompt = int(input("Enter a number: "))
result = prompt * 2
print(f"Result is {result}")

или так

prompt = input("Enter a number: ")
result = int(prompt) * 2
print(f"Result is {result}")

