
1. Напишите функцию, которая принимает два числа в качестве параметра и отображает все нечетные числа между ними.

2. Напишите функцию, которая отображает горизонтальную или вертикальную линию из некоторого символа. Функция принимает в качестве параметра: длину линии, направление, символ.

3. Напишите функцию, которая возвращает максимальное из четырёх чисел. Числа передаются в качестве параметров.

4. Напишите функцию, которая проверяет является ли число простым. Число передаётся в качестве параметра. Если число простое нужно вернуть из метода true, иначе false.

5. Напишите функцию, которая принимет два аргумента: начало и конец диапазона и выводит на экран все простые числа в этом диапазоне. Можно использовать ранее написанную ф-цию в этом задании.

6. Напишите функцию для нахождения максимума в списке целых. Список передаётся в качестве параметра. Можно вернуть через return или просто вывести через print

7. Напишите функцию, определяющую количество чётных, нечётных, положительных, отрицательных элементов списка целых чисел. Список передаётся в качестве пара­метра. Ф-ция возвращает словарь с ключами oddCount, evenCount, positiveCount, negativeCount

8. Напишите функцию, которая принимает список слов и возвращает самое длинное из них.

9. Напишите функцию, которая принимает строку и возвращает словарь, где ключ — буква, а значение — сколько раз она встречается.
Пример: "hello" → {'h': 1, 'e': 1, 'l': 2, 'o': 1}

10. Напишите функцию, которая принимает два списка и возвращает словарь с результатами всех операций:

общие элементы (intersection)

все элементы (union)

симметрическая разность

разность (A - B)

разность (B - A)

Пример:

def someFunction(list1, list2):
    return {
        "intersection": ...,
        "union": ...,
        "sym_diff": ...,
        "a_minus_b": ...,
        "b_minus_a": ...
    }