Класс std::string - современный подход к работе со строками
🎯 После изучения C-style строк пришло время познакомиться с современным подходом! Класс std::string решает большинство проблем, с которыми мы сталкивались при работе с char массивами.

Для того, чтобы использовать класс string нужно сначала подключить соответствующую библиотеку

    #include <string>

Данная строчка добавить новый тип данных string, который на самом деле является классом. Что это такое мы с вами рассмотрим позже, однако начальные понятия о классах у нас уже есть из курса пайтона. В примитивных типах данных мы можем лишь хранить данные и ничего более. Классы же могут иметь в себе и готовые ф-ции, которые могут быть примеными к данным которые хранятся в переменных. Данные ф-ции называются методами и как раз таки класс string уже имеет весь необходимый набор методов для работы со строками в современном стиле. Начнем все по порядку.   

👴 В старом подходе для создания строки мы создавали массив из символов и использовали ф-ции для работы с ними такие как strcat_s и т.п. Сейчас же у нас есть готовый тип данных, куда мы можем записать целую строку. Данный тип данных называется string и вот как он работает

1️⃣ Для создания строки будем использовать точно такой же синтаксис как для создания других примитивных типов данных:


    string text = "Hello world!"

Данный тип данных это лишь обертка работы над массивами символов. Просто команда умных людей решила создать класс который скрывает от нас сложные детали реализации работы массива строк, просто предаставляя нам удобный функционал для комфортной работы. И действительно, в своем внутреннем представлении переменная text будет хранить массив символов, который также заканчивается null terminator-ом. Однако реализация типа string и предлагаемые им возможности делают работу с этим типом более безопасной.

2️⃣ Для вывода данных на консоль будем просто пользоваться cout-om, который также встретив \0 на конце строки перестанет печатать символы.

    cout << text;

Для ввода данных с консоли также можно применять cin.

    string input;
    cout << "Your name => ";
    cin >> input;

    cout << "Hello, " << input << "!" << endl;

⚠️ Однако здесь нужно быть внимательным. Введеный текст через cin cохраняется до 1го пробела. Т.е. если в таком варианте кода я введу Josh Peck, то я сохраню в переменную input только Josh. Чтобы считать всю строку вне зависимости от наличия пробелов, можно применять метод getline()
Метод getline принимает два объекта:

• базовый поток ввода, в нашем случае это cin;  
• переменную, в которую надо считать строку - в нашем случае это input.

    string input;
    cout << "Your name => ";

    getline(cin, input);

    cout << "Hello, " << input << "!" << endl;

3️⃣Для поиска длины строки существуют 2 ф-ции которые являются взаимозаменямыми это length() и size().

    string input;
    cout << "Your name => ";
    getline(cin, input);

    cout << "Hello, " << input << "!" << endl;
    cout << "Length of the name = " << input.size() << "!" << endl;
    cout << "Length of the name = " << input.length() << "!" << endl;

💡 Обратите внимание как я вызвал эти ф-ции. В отличие от ф-ции например sizeof в которую я передавал переменную, данная ф-ция а точнее говоря метод, принадлежит только этому классу и вызывается от имени переменной данного класса\типа.

Я не смогу сделать так:

    int number = 10;
    cout << number.size();

Потому, что int не имеет в своей реализации метод size.

Посмотреть всевозможные методы класса string можно если после переменной данного типа наечатать точку. Среда разработки покажет нам все доступные методы и их сигнатуры.
Так например одним из полезных методов класса является empty(). Данный метод возвращает true/false значение в зависимости от того пустая строка или нет.


    string input;
    cout << "Your name => ";
    getline(cin, input);

    if (input.empty()) 
    {
        cout<< "You didn't enter a name!" << endl;
        return  -1;
    }

    cout << "Hello, " << input << "!" << endl;
    cout << "Length, " << input.length()<< "!" << endl;

    return 0;


‼️ Очень важный момент. У строк есть не только рамер, но и такое свойсто как capacity - это буфер чуть большего размера чем занимают сами символы в строке. Данный буфер как раз таки схож с тем подходом когда мы создавали строку из массива символов чуть большего размера чем нам нужно, подразумевая то, что в дальнейшем мы можем добавить новые символы к ней. К этой теме мы еще вернемся при рассмотрении метода append.

4️⃣ Для конкатенации строк все вообще супер просто. Если у нас есть 2 строки и я хочу их объединить, то в случае С строк я должен был выделить бОльшую память для первой строки, использовать безопасный strcat_s и все такое. В современном подходе все очень просто. Я просто поставлю знак + между строками которые я хочу объединить.  

    string name = "John";
    string surname = "Doe";

    string fullName = name + surname;

    cout << "Full Name: " << fullName << endl;

    return 0;

Что интересно я могу добавить свои собственные литерали при операции конкатенации. Сейчас имя и фамилия слиплись, однако при создании переменной fullname я могу добавить туда и пробел либо любой другой символ или целую строку.

    string name = "John";
    string surname = "Doe";

    string fullName = name + " Malkovich " + surname;

    cout << "Full Name: " << fullName << endl;

    return 0;


5️⃣ Существуют методы в строках позволяющие вам производить преобразование как в так и из строк в различные типы данных. Так например с помощью операции to_string () я могу получить из числа строку.

    int age = 17;
    string ageString = to_string(age);
    string information = "I'm " + ageString + " years old";
    cout << information << endl;

Как видно это уже не метод, а дополнительная ф-ция, котороя доступна из библиотеки string, у которой существует 8 перегрузок под различные типы данных.

Нередко может возникнуть противиположная задача - преобразовать строку в другой тип. Есть ряд функций, которые преобразуют строку в число определенного типа. И для каждого этого типа существует своя определенная ф-ция. Все они начинаются с sto_ и в зависимости от оканчания названия ф-ции компилятор попытается сделать каст в определенный тип даных. Например

    stoi(): преобразует в тип int

    stol(): в long

    stof(): в float

    stod(): в double

💡 Заметьте, что string to short не существует. Разработчики считают, что можно спокойно вызвать stoi, а потом привести к short вручную.

    string ageStr = "25";
    int age = stoi(ageStr);
    age += 5;
    cout << "Age: " << age << endl;	

При неудачном касте программа выдаст ошибку.


6️⃣ Операции сравнения теперь можно производить как с обычными примитивными типами данных. Все мат операции == < > != прменими и здесь. Однако сравнивать можно или 2 string-a или string c литералом. Сравнение все также идет по символьным кодам из таблицы ASCII, по этому результат сравнения также зависит от регистра символов. Также есть аналог strcmp это метод compare который вызывается от имени объекта. В нее передается другая строка, с которой сравнивается текущая. Функция compare возвращает 0, если две строки равны. Если текущая строка больше, то возвращается число больше 0. Если текущая строка меньше, то возвращается число менише 0.

    string a = "Hi";
    string b = "hi";
    bool result = a < b;
    bool compareResult = a.compare (b);

    cout << result;




7️⃣ Получение подстроки

Метод substr() получает подстроку. Этат метод принимает два параметра. Первый параметр представляет индекс, с которого начинается подстрока. Второй параметр - количество символов извлекаемой подстроки. Результатом метода является выделенная строка:

    string source = "k.aslan1707@gmail.com";
    string domain = source.substr(12,5); 5 символов начиная с 12ти включительно.

    cout<< domain << endl; //gmail
    return 0;

Если кол-во извлекаемой части превышает доступную часть, то строка будет извлечена полностью до конца, начиная со стартовой позиции. Никакой ошибки тут не будет.

    string source = "k.aslan1707@gmail.com";
    string domain = source.substr(12,50000);

    cout<< domain << endl; //gmail.com
    return 0;

⚠️ Однако если начальная позиция будет не верной, то мы получим уже ошибку

	string source = "k.aslan1707@gmail.com";
	string domain = source.substr(50000,2);

	cout<< domain << endl;
	return 0;

Второй параметр у этой ф-ции необязательный. Если его не передавать то извлечение пройдет начиная от старта и до конца строки.



    string source = "k.aslan1707@gmail.com";
    string domain = source.substr(2);

    cout<< domain << endl;
    return 0;


8️⃣ Проверки начала и конца строк.
Для удобной работы по сравнению начала и конца строк можно использовать уже готовые методы starts_with и ends_with, которые вызываются от имени проверяемой строки. В параметры ф-ции мы передаем также строку или строковый литерал, который мы хотим проверить. Например

    string source = "k.aslan1707@gmail.com";
    string validDomain = "@gmail.com";
    cout << source.ends_with(".com") << endl;
    cout << source.ends_with(validDomain) << endl;

Аналогично работает и метод starts_with. Под капотом они используют 2 уже рассмотренных метода - substr и compare, чтобы выдернуть необходимую часть строки и сравнить ее. 


9️⃣ Метод find, позволяет нам найти индекс первого вхождения строки или отдельного символа в исходную строку. В отличние от С style строк нам здесь не нужно придумывать велосипед для нахождения индекса. Давайте рассмотрим сразу на примерах:

    string name = "Josh";

    size_t firstIndexOf_O = name.find('o');
    size_t firstIndexOf_Sh = name.find("sh");

    cout << "First index of 'o': " << firstIndexOf_O << endl;
    cout << "First index of \"sh\": " << firstIndexOf_Sh << endl;

Так же как и все остальные операции со строками данный метод также является регистрозависимым. Интересное поведение будет у этого метода, если искомый символ или строка не будет найдена.

    string name = "Josh";

    size_t firstIndexOf_A = name.find('a');
    cout << "First index of 'a': " << firstIndexOf_A << endl;

Здесь я получу огромное число соответствующее max значению unsigned long long-a. Данное значение в библиотеке string выделено в отдельную константу под названием npos. Для доступа к ней нам нужно воспользоваться специальным синтаксисом string::npos. И в дальнейшем я смогу через эту константу проверять найден ли такой индекс или нет.

    string name = "Josh";

    size_t firstIndexOf_A = name.find('a');

    if (firstIndexOf_A == string::npos) 
    {
        cout << "'a' not found in the string." << endl;
        return -1;
    }

    cout << "First index of 'a': " << firstIndexOf_A << endl;


💡 У данного метода есть доп параметры. Так например передав второй параметр я могу указать OffSet - место, с которого я должен искать вхождение символа или подстроки в строку. 
Также есть метод rfind(), который работает аналогично функции find(), принимает те же самые параметры, только ищет подстроку в обратном порядке - с конца строки.


🧠 И теперь перейдем к самому интересному. А именно к изменениям строк. До того как мы узнали про класс string, для изменения строк мы пользовались тем же подходом, что и при изменении массива чисел. Однако теперь, зная класс string мы имеем доступ к уже готовым методам которые облегчат нам работу с модификациями строк.


➕ Для добавления в конец можем использовать уже готовый метод append. Причем через него можно добавлять как и символ, так и целую строку.

    string name = "Josh";
    cout << "Name before append: " << name << endl;

    name.append(" Smith");
    cout << "Name after append: " << name << endl;

В отличие от операции конкатенации в данном случае новая строка не создается, а модифицируется уже существующая. Это во многом возможно за счет capacity string-a. Что происходит в памяти.

🔹 Как работает append

Когда мы вызываем append библиотека проверяет, хватит ли текущей capacity.
Если места достаточно:

    новые символы просто копируются в существующий буфер;

    size увеличивается;

    завершающий \0 ставится в конец.

Если места не хватает:

    выделяется новый буфер большего размера (обычно рост идёт с запасом: x1.5 или x2);

    старая строка копируется туда;

    новые символы добавляются;

    старый буфер освобождается.

Еще один live example

    string s = "Hi";
    cout << "size=" << s.size() << " capacity=" << s.capacity() << endl;

    s.append(" there, how are you?");
    cout << "size=" << s.size() << " capacity=" << s.capacity() << endl;

Добавляя знаки вопроса в конец мы можем заметить что при достижении предела capacity увеличиться в 1.5 раза.


➕ Операция insert позволяет вставлять данные в определенную позицию а не только в конец. Данный метод принимет индекс вставки и вставляемую строку:

    string s = "ABCDEFG";
    

    s.insert(1, "XYZ");
    cout << s << endl;

Тут все идет по тому же сценарию, что и в случае с append-ом. Меняются size и capacity (если нужно).

⚠️ При попытке вставки элемента в невалидный индекс получим ошибку  

    string s = "ABCDEFG";
    s.insert(-1, "XYZ");    ❌
    s.insert(1000, "XYZ");  ❌


🗑️ И наконец для удаления символов из строки существует  метод erase. В качестве параметров он принимает начальный индекс удаления и количество удаляемых символов.


    string message =  "Hello world";
    message.erase(0, 6);       // с 5-го индекса удаляем 4 символа 
    cout << message << endl; 

Если в функцию не передается никаких параметров, то она удаляет все символы, и в результате получаем пустую строку:

    string message =  "Hello world";
    message.erase();

Кроме метода erase есть одноименная ф-ция, которая вызывается не от имени объекта. Она принимает строку и символ, в итоге удаляя все вхождения данного символа с исходную строку. Удалить вхождения подстрок с помощью ф-ции erase нельзя.

    string message =  "Hello world";
    erase(message, 'o');
    cout << message << endl; 



🎯 Практические задачи

📝 Практические задачи по теме std::string
🔹 Уровень 1 (базовый)

Работа с вводом, выводом, длиной и конкатенацией строк.

1. Ввод имени и проверка пустой строки
Считайте строку с помощью getline.
Если строка пустая — вывести "You didn't enter a name!", иначе "Hello, <имя>".

2. Подсчет длины строки
Считайте предложение.
Выведите:
 1. количество символов (length());
 2. количество символов без пробелов.

3. Конкатенация строк
Считайте имя и фамилию, объедините их в fullName через пробел.
Выведите результат:
"Your full name is: John Doe"

4. Приветствие с возрастом
Считайте имя (строка) и возраст (целое число).
Используйте to_string, чтобы вывести строку вида:
"Hello, John! You are 20 years old."

5. Первые и последние буквы
Считайте строку.
Выведите первый и последний символ строки.
Если строка пустая — выведите "Empty string".

🔹 Уровень 2 (средний)

Использование методов поиска, подстрок и проверки начала/конца.

6. Проверка email-адреса
Пусть пользователь вводит email.
Проверьте:

1. заканчивается ли он на @gmail.com.
2. выведите "Valid gmail address" или "Not a gmail address".

7. Поиск подстроки
Считайте строку у пользователя.
Спросите отдельное слово и проверьте, входит ли оно в строку (через find).
Выведите позицию или "Word not found".

8. Извлечение домена из email
Пусть дана строка "student123@university.edu".
Извлеките часть после @ ("university.edu") и выведите её.

9. Проверка начала строки
Считайте строку и проверьте, начинается ли она с "https://" (метод starts_with).
Если да — выведите "This is a valid URL", иначе "Invalid URL".

10. Подсчет гласных
Считайте строку и подсчитайте количество гласных букв (a, e, i, o, u + их заглавные варианты).

🔹 Уровень 3 (продвинутый)

Изменение строк и работа с преобразованием типов.

11. Преобразование типов
Пусть пользователь вводит число в виде строки.
Преобразуйте его в int (stoi), увеличьте на 10, снова превратите в строку (to_string).
Выведите "Number + 10 = <результат>".

12. Замена символов
Считайте строку и замените все пробелы символом _.

13. Редактор строки
Считайте строку "ABCDEFG".

Вставьте "XYZ" после второго символа.

Удалите первые 3 символа.

Выведите итоговую строку.

14. Подсчет слов
Считайте строку (предложение).
Посчитайте количество слов в строке (слово — это последовательность символов, разделенных пробелами).

15. Частота символов
Считайте строку.
Посчитайте, сколько раз встречается каждый символ, и выведите результат в формате: